#!/usr/bin/env bash

if [ $# -lt 1 ]; then
    echo "enable or disable"
    exit 1
fi

if [ "enable" = "$1" ]; then
    chmod 777 /tmp/tmux-socket
elif [ "disable" = "$1" ]; then
    chmod 770 /tmp/tmux-socket
else
    echo "enable or disable"
    exit 1
fi
